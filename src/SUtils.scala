import java.net.URI
import soundness.*
import contingency.strategies.throwUnsafely

object SUtils {
  given Online = nettlesome.Online
  def getSize(url: URI) =
    // val headResult = Url.parse(url.toString.tt).head()
    ???
}
